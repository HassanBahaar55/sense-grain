import PredictionsPage from '@/features/predictions/PredictionsPage';

export const metadata = { title: 'Predictions — Sense Grain' };

export default function Page() {
  return <PredictionsPage />;
}
