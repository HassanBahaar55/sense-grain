import StorageUnitsPage from '@/features/storage/StorageUnitsPage';

export const metadata = { title: 'Storage Units — Sense Grain' };

export default function Page() {
  return <StorageUnitsPage />;
}
