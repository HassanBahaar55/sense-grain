{
  "name": "@sense-grain/shared-design",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens and UI foundations for Sense Grain.",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  }
}
